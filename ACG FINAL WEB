<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Aegis Compliance Group — Your Shield for Smarter Solutions</title>
<link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&family=DM+Serif+Display:ital@0;1&display=swap" rel="stylesheet">
<style>
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  :root {
    --teal: #1a8a96;
    --teal-dark: #136e78;
    --teal-light: #e8f6f7;
    --teal-mid: #2aaabb;
    --charcoal: #2d2d2d;
    --grey: #6b6b6b;
    --off-white: #f7f7f5;
    --text: #2d2d2d;
    --text-muted: #6b6b6b;
    --border: #e5e7eb;
  }

  html { scroll-behavior: smooth; }

  body {
    font-family: 'Outfit', sans-serif;
    color: var(--text);
    background: #fff;
    line-height: 1.6;
    font-size: 16px;
  }

  /* NAV */
  nav {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 100;
    background: rgba(255,255,255,0.97);
    backdrop-filter: blur(8px);
    border-bottom: 1px solid var(--border);
    padding: 0 5%;
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 130px;
  }

  .nav-logo img {
    height: 120px;
    width: auto;
    display: block;
  }

  .nav-links {
    display: flex;
    gap: 36px;
    list-style: none;
    align-items: center;
  }

  .nav-links a {
    font-size: 14px;
    font-weight: 400;
    color: var(--text-muted);
    text-decoration: none;
    transition: color 0.2s;
  }

  .nav-links a:hover { color: var(--teal); }

  .nav-cta {
    background: var(--teal) !important;
    color: #fff !important;
    padding: 10px 22px;
    border-radius: 6px;
    font-weight: 500 !important;
    transition: background 0.2s !important;
  }

  .nav-cta:hover { background: var(--teal-dark) !important; }

  /* HERO */
  .hero {
    padding: 200px 5% 100px;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 80px;
    align-items: center;
    max-width: 1200px;
    margin: 0 auto;
  }

  .hero-eyebrow {
    font-size: 12px;
    font-weight: 500;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--teal);
    margin-bottom: 20px;
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .hero-eyebrow::before {
    content: '';
    display: block;
    width: 32px;
    height: 1.5px;
    background: var(--teal);
  }

  .hero h1 {
    font-family: 'DM Serif Display', serif;
    font-size: clamp(38px, 4.5vw, 56px);
    line-height: 1.1;
    color: var(--charcoal);
    margin-bottom: 24px;
    letter-spacing: -0.02em;
  }

  .hero h1 em {
    font-style: italic;
    color: var(--teal);
  }

  .hero-desc {
    font-size: 17px;
    color: var(--text-muted);
    line-height: 1.7;
    margin-bottom: 40px;
    max-width: 480px;
  }

  .hero-actions {
    display: flex;
    gap: 16px;
    align-items: center;
  }

  .btn-primary {
    background: var(--teal);
    color: #fff;
    padding: 14px 30px;
    border-radius: 6px;
    text-decoration: none;
    font-weight: 500;
    font-size: 15px;
    transition: background 0.2s, transform 0.15s;
    display: inline-block;
  }

  .btn-primary:hover { background: var(--teal-dark); transform: translateY(-1px); }

  .btn-secondary {
    color: var(--charcoal);
    font-weight: 500;
    font-size: 15px;
    text-decoration: none;
    display: flex;
    align-items: center;
    gap: 8px;
    transition: gap 0.2s;
  }

  .btn-secondary:hover { gap: 12px; color: var(--teal); }
  .btn-secondary::after { content: '→'; }

  .hero-visual { position: relative; }

  .hero-card {
    background: var(--charcoal);
    border-radius: 16px;
    padding: 48px 40px;
    color: #fff;
    position: relative;
    overflow: hidden;
  }

  .hero-card::before {
    content: '';
    position: absolute;
    top: -40px; right: -40px;
    width: 200px; height: 200px;
    background: var(--teal);
    opacity: 0.15;
    border-radius: 50%;
  }

  .hero-card-label {
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--teal-mid);
    margin-bottom: 32px;
  }

  .hero-stat {
    margin-bottom: 28px;
    padding-bottom: 28px;
    border-bottom: 1px solid rgba(255,255,255,0.1);
  }

  .hero-stat:last-child { border-bottom: none; margin-bottom: 0; padding-bottom: 0; }

  .hero-stat-num {
    font-family: 'DM Serif Display', serif;
    font-size: 38px;
    color: #fff;
    line-height: 1;
    margin-bottom: 4px;
  }

  .hero-stat-label { font-size: 13px; color: rgba(255,255,255,0.5); }

  /* TRUST BAR */
  .trust-bar {
    background: var(--off-white);
    padding: 28px 5%;
    text-align: center;
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
  }

  .trust-bar p {
    font-size: 13px;
    font-weight: 500;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--text-muted);
  }

  .trust-bar-items {
    display: flex;
    justify-content: center;
    gap: 48px;
    margin-top: 16px;
    flex-wrap: wrap;
  }

  .trust-item {
    font-size: 14px;
    font-weight: 500;
    color: var(--charcoal);
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .trust-item::before { content: '✓'; color: var(--teal); font-weight: 700; }

  /* SERVICES */
  .section {
    padding: 100px 5%;
    max-width: 1200px;
    margin: 0 auto;
  }

  .section-eyebrow {
    font-size: 12px;
    font-weight: 500;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--teal);
    margin-bottom: 16px;
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .section-eyebrow::before {
    content: '';
    display: block;
    width: 32px;
    height: 1.5px;
    background: var(--teal);
  }

  .section-title {
    font-family: 'DM Serif Display', serif;
    font-size: clamp(30px, 3.5vw, 44px);
    color: var(--charcoal);
    line-height: 1.15;
    letter-spacing: -0.02em;
    max-width: 520px;
    margin-bottom: 16px;
  }

  .section-subtitle {
    font-size: 16px;
    color: var(--text-muted);
    max-width: 560px;
    margin-bottom: 60px;
    line-height: 1.7;
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2px;
    background: var(--border);
    border: 1px solid var(--border);
    border-radius: 12px;
    overflow: hidden;
  }

  .service-card {
    background: #fff;
    padding: 40px 36px;
    transition: background 0.2s;
  }

  .service-card:hover { background: var(--teal-light); }

  .service-num {
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.1em;
    color: var(--teal);
    margin-bottom: 20px;
  }

  .service-icon {
    width: 44px;
    height: 44px;
    margin-bottom: 20px;
  }

  .service-name {
    font-size: 18px;
    font-weight: 600;
    color: var(--charcoal);
    margin-bottom: 12px;
    line-height: 1.3;
  }

  .service-desc {
    font-size: 14px;
    color: var(--text-muted);
    line-height: 1.7;
  }

  /* WHY ACG */
  .why-section {
    background: var(--charcoal);
    padding: 100px 5%;
  }

  .why-inner {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 80px;
    align-items: center;
  }

  .why-section .section-eyebrow { color: var(--teal-mid); }
  .why-section .section-eyebrow::before { background: var(--teal-mid); }
  .why-section .section-title { color: #fff; max-width: 100%; }
  .why-section .section-subtitle { color: rgba(255,255,255,0.55); }

  .why-points { display: flex; flex-direction: column; gap: 32px; }

  .why-point { display: flex; gap: 20px; align-items: flex-start; }

  .why-point-icon {
    width: 44px;
    height: 44px;
    background: rgba(26,138,150,0.2);
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .why-point-icon svg { width: 22px; height: 22px; stroke: var(--teal-mid); fill: none; stroke-width: 1.8; }

  .why-point-title { font-size: 16px; font-weight: 600; color: #fff; margin-bottom: 6px; }
  .why-point-desc { font-size: 14px; color: rgba(255,255,255,0.5); line-height: 1.65; }

  /* CONTACT */
  .contact-section { padding: 100px 5%; background: var(--off-white); }

  .contact-inner {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 80px;
    align-items: start;
  }

  .contact-form {
    background: #fff;
    border-radius: 12px;
    padding: 48px;
    border: 1px solid var(--border);
  }

  .form-group { margin-bottom: 20px; }

  .form-group label {
    display: block;
    font-size: 13px;
    font-weight: 500;
    color: var(--charcoal);
    margin-bottom: 8px;
    letter-spacing: 0.03em;
  }

  .form-group input,
  .form-group select,
  .form-group textarea {
    width: 100%;
    padding: 12px 16px;
    border: 1px solid var(--border);
    border-radius: 6px;
    font-family: 'Outfit', sans-serif;
    font-size: 14px;
    color: var(--text);
    background: #fff;
    transition: border-color 0.2s;
    outline: none;
  }

  .form-group input:focus,
  .form-group select:focus,
  .form-group textarea:focus { border-color: var(--teal); }

  .form-group textarea { height: 120px; resize: vertical; }

  .form-submit {
    width: 100%;
    background: var(--teal);
    color: #fff;
    border: none;
    padding: 14px;
    border-radius: 6px;
    font-family: 'Outfit', sans-serif;
    font-size: 15px;
    font-weight: 500;
    cursor: pointer;
    transition: background 0.2s;
    margin-top: 8px;
  }

  .form-submit:hover { background: var(--teal-dark); }

  .contact-info { padding-top: 8px; }

  .contact-detail {
    display: flex;
    gap: 16px;
    align-items: flex-start;
    margin-bottom: 32px;
  }

  .contact-detail-icon {
    width: 44px;
    height: 44px;
    background: var(--teal-light);
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .contact-detail-icon svg { width: 20px; height: 20px; stroke: var(--teal); fill: none; stroke-width: 1.8; }

  .contact-detail-label {
    font-size: 12px;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--text-muted);
    margin-bottom: 4px;
  }

  .contact-detail-value { font-size: 15px; font-weight: 500; color: var(--charcoal); }

  /* FOOTER */
  footer {
    background: var(--charcoal);
    padding: 40px 5%;
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 16px;
  }

  .footer-logo img { height: 40px; width: auto; }
  .footer-tagline { font-size: 12px; color: rgba(255,255,255,0.4); margin-top: 6px; }
  .footer-right { font-size: 13px; color: rgba(255,255,255,0.4); }

  @media (max-width: 768px) {
    .hero { grid-template-columns: 1fr; gap: 48px; }
    .services-grid { grid-template-columns: 1fr; }
    .why-inner { grid-template-columns: 1fr; }
    .contact-inner { grid-template-columns: 1fr; }
    nav .nav-links { display: none; }
  }
</style>
</head>
<body>

<!-- NAV -->
<nav>
  <a href="#" class="nav-logo">
    <img src="Aegis.png" alt="Aegis Compliance Group">
  </a>
  <ul class="nav-links">
    <li><a href="#services">Services</a></li>
    <li><a href="#why">Why ACG</a></li>
    <li><a href="#contact">Contact</a></li>
    <li><a href="#contact" class="nav-cta">Get a Quote</a></li>
  </ul>
</nav>

<!-- HERO -->
<section style="background:#fff;">
  <div class="hero">
    <div class="hero-content">
      <p class="hero-eyebrow">UK Construction Compliance</p>
      <h1>Compliance that <em>protects</em> your projects</h1>
      <p class="hero-desc">Aegis Compliance Group provides AI-powered compliance documentation, risk assessments, and estimating services for contractors, FM companies, and principal contractors across the UK.</p>
      <div class="hero-actions">
        <a href="#contact" class="btn-primary">Get a free quote</a>
        <a href="#services" class="btn-secondary">Our services</a>
      </div>
    </div>
    <div class="hero-visual">
      <div class="hero-card">
        <p class="hero-card-label">What we deliver</p>
        <div class="hero-stat">
          <div class="hero-stat-num">100%</div>
          <div class="hero-stat-label">CDM &amp; HSE compliant documentation</div>
        </div>
        <div class="hero-stat">
          <div class="hero-stat-num">48hr</div>
          <div class="hero-stat-label">Turnaround on compliance packs</div>
        </div>
        <div class="hero-stat">
          <div class="hero-stat-num">AI-first</div>
          <div class="hero-stat-label">Faster, smarter, more accurate</div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- TRUST BAR -->
<div class="trust-bar">
  <p>Trusted by contractors working with</p>
  <div class="trust-bar-items">
    <span class="trust-item">Local Authorities</span>
    <span class="trust-item">NHS Trusts</span>
    <span class="trust-item">Housing Associations</span>
    <span class="trust-item">Schools &amp; Academies</span>
    <span class="trust-item">FM Companies</span>
  </div>
</div>

<!-- SERVICES -->
<div style="background:#fff;">
  <div class="section" id="services">
    <p class="section-eyebrow">What we do</p>
    <h2 class="section-title">Everything your project needs to stay compliant</h2>
    <p class="section-subtitle">From method statements to full compliance packs — we handle the paperwork so you can focus on the build.</p>

    <div class="services-grid">
      <div class="service-card">
        <p class="service-num">01</p>
        <svg class="service-icon" viewBox="0 0 44 44" fill="none">
          <rect width="44" height="44" rx="10" fill="#e8f6f7"/>
          <path d="M14 22h16M14 16h10M14 28h12" stroke="#1a8a96" stroke-width="1.8" stroke-linecap="round"/>
          <rect x="28" y="26" width="8" height="8" rx="2" stroke="#1a8a96" stroke-width="1.5" fill="none"/>
          <path d="M30 30h4M32 28v4" stroke="#1a8a96" stroke-width="1.2" stroke-linecap="round"/>
        </svg>
        <h3 class="service-name">Method Statements</h3>
        <p class="service-desc">Professionally written, site-specific method statements that meet principal contractor and client requirements first time.</p>
      </div>

      <div class="service-card">
        <p class="service-num">02</p>
        <svg class="service-icon" viewBox="0 0 44 44" fill="none">
          <rect width="44" height="44" rx="10" fill="#e8f6f7"/>
          <path d="M22 14v4M22 26v4M14 22h4M26 22h4" stroke="#1a8a96" stroke-width="1.8" stroke-linecap="round"/>
          <circle cx="22" cy="22" r="6" stroke="#1a8a96" stroke-width="1.5" fill="none"/>
          <circle cx="22" cy="22" r="2" fill="#1a8a96"/>
        </svg>
        <h3 class="service-name">Risk Assessments</h3>
        <p class="service-desc">Thorough, task-specific risk assessments aligned with HSE guidelines and your site conditions.</p>
      </div>

      <div class="service-card">
        <p class="service-num">03</p>
        <svg class="service-icon" viewBox="0 0 44 44" fill="none">
          <rect width="44" height="44" rx="10" fill="#e8f6f7"/>
          <path d="M16 28l4-8 4 6 2-4 4 6" stroke="#1a8a96" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
          <rect x="13" y="13" width="18" height="18" rx="4" stroke="#1a8a96" stroke-width="1.5" fill="none"/>
        </svg>
        <h3 class="service-name">COSHH Assessments</h3>
        <p class="service-desc">Complete COSHH documentation for all substances used on site, keeping your workforce and clients protected.</p>
      </div>

      <div class="service-card">
        <p class="service-num">04</p>
        <svg class="service-icon" viewBox="0 0 44 44" fill="none">
          <rect width="44" height="44" rx="10" fill="#e8f6f7"/>
          <path d="M22 14l6 3v6c0 4-3 6-6 7-3-1-6-3-6-7v-6l6-3z" stroke="#1a8a96" stroke-width="1.5" fill="none"/>
          <path d="M19 22l2 2 4-4" stroke="#1a8a96" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
        <h3 class="service-name">Compliance Packs</h3>
        <p class="service-desc">Full pre-construction and in-progress compliance packs bundled and ready for submission to your client or principal contractor.</p>
      </div>

      <div class="service-card">
        <p class="service-num">05</p>
        <svg class="service-icon" viewBox="0 0 44 44" fill="none">
          <rect width="44" height="44" rx="10" fill="#e8f6f7"/>
          <path d="M14 30l5-10 4 6 3-4 4 8" stroke="#1a8a96" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
          <path d="M13 14h18" stroke="#1a8a96" stroke-width="1.5" stroke-linecap="round"/>
        </svg>
        <h3 class="service-name">Estimating Support</h3>
        <p class="service-desc">Accurate quantity surveying and cost estimating support for tenders, helping you price competitively and win more work.</p>
      </div>

      <div class="service-card">
        <p class="service-num">06</p>
        <svg class="service-icon" viewBox="0 0 44 44" fill="none">
          <rect width="44" height="44" rx="10" fill="#e8f6f7"/>
          <circle cx="22" cy="20" r="5" stroke="#1a8a96" stroke-width="1.5" fill="none"/>
          <path d="M14 31c0-4 3.6-7 8-7s8 3 8 7" stroke="#1a8a96" stroke-width="1.8" stroke-linecap="round" fill="none"/>
        </svg>
        <h3 class="service-name">Consultancy</h3>
        <p class="service-desc">Ongoing compliance consultancy and advice for contractors who need a reliable expert in their corner, without the overhead.</p>
      </div>
    </div>
  </div>
</div>

<!-- WHY ACG -->
<section class="why-section" id="why">
  <div class="why-inner">
    <div>
      <p class="section-eyebrow">Why choose ACG</p>
      <h2 class="section-title" style="color:#fff;">Built for contractors, not boardrooms</h2>
      <p class="section-subtitle">We understand the pressures of construction. Fast turnarounds, real site experience, and documentation that actually gets approved.</p>
    </div>
    <div class="why-points">
      <div class="why-point">
        <div class="why-point-icon">
          <svg viewBox="0 0 24 24"><path d="M13 10V3L4 14h7v7l9-11h-7z"/></svg>
        </div>
        <div>
          <p class="why-point-title">AI-powered, expert reviewed</p>
          <p class="why-point-desc">We combine the speed of AI with real QS and site supervisor knowledge to deliver documentation that's accurate and compliant.</p>
        </div>
      </div>
      <div class="why-point">
        <div class="why-point-icon">
          <svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/></svg>
        </div>
        <div>
          <p class="why-point-title">48-hour turnaround</p>
          <p class="why-point-desc">Need documents fast? We deliver compliance packs within 48 hours so you never lose work due to missing paperwork.</p>
        </div>
      </div>
      <div class="why-point">
        <div class="why-point-icon">
          <svg viewBox="0 0 24 24"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
        </div>
        <div>
          <p class="why-point-title">Sector experience</p>
          <p class="why-point-desc">We work with contractors serving local authorities, NHS trusts, housing associations, and schools — we know what your clients expect.</p>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- CONTACT -->
<section class="contact-section" id="contact">
  <div class="contact-inner">
    <div class="contact-info">
      <p class="section-eyebrow">Get in touch</p>
      <h2 class="section-title">Ready to protect your next project?</h2>
      <p class="section-subtitle">Tell us about your project and we'll get back to you with a quote within 24 hours.</p>

      <div class="contact-detail">
        <div class="contact-detail-icon">
          <svg viewBox="0 0 24 24"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/><polyline points="22,6 12,13 2,6"/></svg>
        </div>
        <div>
          <p class="contact-detail-label">Email</p>
          <p class="contact-detail-value">benreveley@aegiscompliancegroup.co.uk</p>
        </div>
      </div>

      <div class="contact-detail">
        <div class="contact-detail-icon">
          <svg viewBox="0 0 24 24"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0118 0z"/><circle cx="12" cy="10" r="3"/></svg>
        </div>
        <div>
          <p class="contact-detail-label">Based in</p>
          <p class="contact-detail-value">Hull, East Yorkshire — serving clients nationwide</p>
        </div>
      </div>
    </div>

    <div class="contact-form">
      <h3 style="font-size:20px; font-weight:600; color:var(--charcoal); margin-bottom:28px;">Request a free quote</h3>
      <div class="form-group">
        <label>Your name</label>
        <input type="text" placeholder="John Smith">
      </div>
      <div class="form-group">
        <label>Company name</label>
        <input type="text" placeholder="Smith Contractors Ltd">
      </div>
      <div class="form-group">
        <label>Email address</label>
        <input type="email" placeholder="john@smithcontractors.co.uk">
      </div>
      <div class="form-group">
        <label>Service required</label>
        <select>
          <option value="">Select a service...</option>
          <option>Method Statement</option>
          <option>Risk Assessment</option>
          <option>COSHH Assessment</option>
          <option>Full Compliance Pack</option>
          <option>Estimating Support</option>
          <option>Consultancy</option>
        </select>
      </div>
      <div class="form-group">
        <label>Tell us about your project</label>
        <textarea placeholder="Brief description of the work, location, and timescales..."></textarea>
      </div>
      <button class="form-submit">Send enquiry →</button>
    </div>
  </div>
</section>

<!-- FOOTER -->
<footer>
  <div>
    <div class="footer-logo" style="font-family:'DM Serif Display',serif; font-size:20px; color:#fff; font-weight:400;">Aegis Compliance Group</div>
    <div class="footer-tagline">Your Shield for Smarter Solutions</div>
  </div>
  <div class="footer-right">© 2026 Aegis Compliance Group. All rights reserved.</div>
</footer>

</body>
</html>
